import type { WorkflowRunStorage } from "./workflow-storage.ts";
export type WorkflowBinding={create(input:{id:string;params:{runId:string}}):Promise<{id:string}>};
export async function ensureWorkflowLaunched(storage:WorkflowRunStorage,binding:WorkflowBinding,runId:string,generation:number){const proposed=`${runId}-g${generation}`,claim=await storage.claimWorkflowLaunch(runId,generation,proposed);if(claim.result==="terminal")throw new Error("invalid_launch_state");if(claim.result==="already_attached"||claim.result==="already_launching")return {instanceId:claim.instanceId,launched:false};try{const instance=await binding.create({id:claim.instanceId,params:{runId}});await storage.attachWorkflowInstance(runId,generation,claim.instanceId);return {instanceId:instance.id,launched:true};}catch(error){const duplicate=error&&typeof error==="object"&&("status" in error&&error.status===409||"code" in error&&error.code==="instance_already_exists");if(duplicate){await storage.attachWorkflowInstance(runId,generation,claim.instanceId);return {instanceId:claim.instanceId,launched:false};}await storage.recordWorkflowLaunchFailure(runId,generation,claim.instanceId,"The durable workflow could not be started.");throw Object.assign(new Error("workflow_launch_unavailable"),{code:"workflow_launch_unavailable"});}}
