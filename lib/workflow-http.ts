import { NextResponse } from "next/server";import { noStoreHeaders } from "./auth-core.ts";import { DefinitionConflictError,DefinitionNotFoundError } from "./workflow-definition-repository.ts";
export async function readBoundedJson(request:Request,max=131072){const declared=Number(request.headers.get("content-length")??0);if(declared>max)throw new Error("body_too_large");const text=await request.text();if(Buffer.byteLength(text,"utf8")>max)throw new Error("body_too_large");try{return JSON.parse(text) as unknown;}catch{throw new Error("invalid_json");}}
export function workflowError(error:unknown){if(error instanceof DefinitionConflictError)return NextResponse.json({error:error.message,code:"conflict"},{status:409,headers:noStoreHeaders});if(error instanceof DefinitionNotFoundError)return NextResponse.json({error:error.message,code:"not_found"},{status:404,headers:noStoreHeaders});const code=error instanceof Error&&["body_too_large","invalid_json"].includes(error.message)?error.message:"operation_failed";return NextResponse.json({error:code==="body_too_large"?"Request body is too large.":code==="invalid_json"?"Request body is invalid.":"The workflow operation could not be completed.",code},{status:code==="body_too_large"?413:code==="invalid_json"?400:500,headers:noStoreHeaders});}
