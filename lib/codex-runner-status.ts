import {CodexRunnerError,getCodexRunnerClient,type RunnerAuthStatus,type RunnerCapabilities} from "./codex-runner-client";
export type CodexConnectionState="configuration-missing"|"unavailable"|"update-required"|"disconnected"|"waiting"|"connected";
export interface SafeCodexConnectionStatus{state:CodexConnectionState;label:string;capabilities?:RunnerCapabilities;auth?:RunnerAuthStatus}
export async function getSafeCodexConnectionStatus():Promise<SafeCodexConnectionStatus>{try{const client=getCodexRunnerClient(),capabilities=await client.capabilities();if(!capabilities.deviceAuth)return{state:"update-required",label:"Runner update required",capabilities};const auth=await client.authStatus();return auth.connected?{state:"connected",label:"Connected to ChatGPT",capabilities,auth}:{state:"disconnected",label:"Runner ready — ChatGPT not connected",capabilities,auth};}catch(error){if(error instanceof CodexRunnerError&&error.category==="configuration_missing")return{state:"configuration-missing",label:"Runner configuration missing"};if(error instanceof CodexRunnerError&&error.category==="runner_update_required")return{state:"update-required",label:"Runner update required"};return{state:"unavailable",label:"Runner unavailable"}}}
