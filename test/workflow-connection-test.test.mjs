import test from 'node:test';import assert from 'node:assert/strict';import {testWorkflowConnection} from '../lib/workflow-connection-test.ts';import {workIqAuthenticationFailure} from '../lib/work-iq-auth.ts';
test('delegated resolver failures preserve bounded connection-test categories',async()=>{for(const category of ['authentication_failed','provider_unavailable','provider_timeout','permission_denied']){const result=await testWorkflowConnection('work',{resolveForExecution:async()=>{throw workIqAuthenticationFailure(category)}},new Map());assert.equal(result.category,category);assert.doesNotMatch(JSON.stringify(result),/token|error_description|upstream body/i)}});
test('uncategorized and API-key resolver failures retain the existing unavailable result',async()=>{assert.deepEqual(await testWorkflowConnection('openai',{resolveForExecution:async()=>{throw new Error('raw')}},new Map()),{ok:false,category:'connection_unavailable',safeMessage:'Connection is not configured.'})});
