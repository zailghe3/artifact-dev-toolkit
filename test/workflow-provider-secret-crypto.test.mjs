import test from'node:test';import assert from'node:assert/strict';import{encryptProviderCredential,decryptProviderCredential}from'../lib/workflow-provider-secret-crypto.ts';
const root=Buffer.alloc(32,7).toString('base64'),other=Buffer.alloc(32,8).toString('base64'),value='unit-test-provider-credential-7f31';
test('provider credential encryption is randomized, authenticated, and fail closed',async()=>{const a=await encryptProviderCredential(value,root,'openai-primary','openai-responses'),b=await encryptProviderCredential(value,root,'openai-primary','openai-responses');assert.notEqual(a.ciphertext,value);assert.notDeepEqual(a,b);assert.equal(await decryptProviderCredential(a,root,'openai-primary','openai-responses'),value);for(const args of [[a,other,'openai-primary','openai-responses'],[{...a,iv:Buffer.alloc(12).toString('base64')},root,'openai-primary','openai-responses'],[{...a,ciphertext:Buffer.from(a.ciphertext,'base64').map((x,i)=>i?x:x^1).toString('base64')},root,'openai-primary','openai-responses'],[a,root,'changed','openai-responses'],[a,root,'openai-primary','changed'],[{...a,version:2},root,'openai-primary','openai-responses']])await assert.rejects(()=>decryptProviderCredential(...args));});
