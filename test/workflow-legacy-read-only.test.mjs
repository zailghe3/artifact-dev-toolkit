import test from 'node:test';import assert from 'node:assert/strict';import {isLegacyRunReadOnly,legacyRunReadOnlyReason} from '../lib/workflow-run-legacy.ts';
const vault={management:'git',credentialSource:'adt-vault'};const generic={engineVersion:'2',executionPlan:{planVersion:2},connectionSnapshots:{main:vault}};
test('current generic vault-backed graph runs remain executable',()=>assert.equal(isLegacyRunReadOnly(generic),false));
test('retired persisted execution and credential formats are centrally read only',()=>{assert.equal(legacyRunReadOnlyReason({...generic,engineVersion:'1'}),'engine_v1');assert.equal(legacyRunReadOnlyReason({...generic,executionPlan:{nodes:[]}}),'historical_execution_plan');assert.equal(legacyRunReadOnlyReason({...generic,connectionSnapshots:{main:{management:'d1'}}}),'d1_provider_configuration');assert.equal(legacyRunReadOnlyReason({...generic,connectionSnapshots:{main:{management:'git',credentialSource:'cloudflare-binding'}}}),'cloudflare_binding_credential')});
