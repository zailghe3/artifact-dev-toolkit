import test from "node:test";import assert from "node:assert/strict";import {installTsxHook} from "./render-tsx.mjs";const require=installTsxHook(),{evaluateRunnerCompatibility}=require("../lib/codex-runner-compatibility.ts");
const installed=(runnerRevision,patch={})=>({protocolVersion:1,runnerRevision,codexVersion:"0.147.0",runnerVersion:"8".repeat(40),...patch});
test("compatibility and freshness are independent",()=>{assert.deepEqual(evaluateRunnerCompatibility(installed(1)),{protocol:"compatible",runnerRevision:"current",codexVersion:"current"});assert.deepEqual(evaluateRunnerCompatibility(installed(0)),{protocol:"compatible",runnerRevision:"update_available",codexVersion:"current"});assert.deepEqual(evaluateRunnerCompatibility(installed(2)),{protocol:"compatible",runnerRevision:"runner_newer_than_adt",codexVersion:"current"});assert.equal(evaluateRunnerCompatibility(installed(1,{protocolVersion:2})).protocol,"incompatible");assert.equal(evaluateRunnerCompatibility(installed(1,{codexVersion:"0.146.0"})).codexVersion,"mismatch")});
test("different application and Runner SHAs do not affect current status",()=>{const result=evaluateRunnerCompatibility(installed(1,{runnerVersion:"8".repeat(40),applicationBuild:"a".repeat(40)}));assert.equal(result.protocol,"compatible");assert.equal(result.runnerRevision,"current")});
test("missing facts never become current",()=>assert.deepEqual(evaluateRunnerCompatibility(undefined),{protocol:"unknown",runnerRevision:"unknown",codexVersion:"unknown"}));
