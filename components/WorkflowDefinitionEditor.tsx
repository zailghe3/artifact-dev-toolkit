"use client";import {useState} from "react";import {useRouter} from "next/navigation";
type Agent={id:string;name:string};type Initial={id:string;name:string;description:string;steps:Array<{agentId:string}>};
export function WorkflowDefinitionEditor({agents,initial,fileSha}:{agents:Agent[];initial?:Initial;fileSha?:string}){const router=useRouter(),[steps,setSteps]=useState<string[]>(initial?.steps.map(s=>s.agentId)??[agents[0]?.id??""]),[error,setError]=useState("");const move=(i:number,d:number)=>setSteps(old=>{const copy=[...old],[item]=copy.splice(i,1);copy.splice(i+d,0,item);return copy});async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget),definition={schemaVersion:1,id:String(data.get("id")),name:String(data.get("name")),description:String(data.get("description")),status:"draft",steps:steps.map((agentId,index)=>({id:`step-${index+1}`,name:agents.find(a=>a.id===agentId)?.name??`Step ${index+1}`,agentId,input:{source:index?"previous_step":"run_input"},onSuccess:{type:index===steps.length-1?"complete":"next"},onFailure:{type:"fail"}})),result:{source:"step_output",stepId:`step-${steps.length}`},limits:{maxStepExecutions:Math.max(steps.length,32)}};const response=await fetch(initial?`/api/workflow-definitions/${initial.id}`:"/api/workflow-definitions",{method:initial?"PUT":"POST",headers:{"content-type":"application/json"},body:JSON.stringify(initial?{definition,fileSha}:definition)});if(!response.ok){setError(((await response.json()) as {error?:string}).error??"Save failed.");return;}router.push(`/workflows/definitions/${definition.id}`);router.refresh();}return <form onSubmit={submit} className="mt-6 grid max-w-2xl gap-4">{error&&<p role="alert">{error}</p>}<label>ID<input name="id" required readOnly={Boolean(initial)} defaultValue={initial?.id} className="block w-full rounded border p-2"/></label><label>Name<input name="name" required defaultValue={initial?.name} className="block w-full rounded border p-2"/></label><label>Description<textarea name="description" defaultValue={initial?.description} className="block w-full rounded border p-2"/></label><fieldset><legend className="font-bold">Ordered agents</legend>{steps.map((value,index)=><div className="mt-3 flex items-center gap-2" key={index}><select aria-label={`Agent for step ${index+1}`} value={value} onChange={e=>setSteps(old=>old.map((v,i)=>i===index?e.target.value:v))}>{agents.map(agent=><option key={agent.id} value={agent.id}>{agent.name}</option>)}</select><span>{index===0?"Receives run input":"Receives exact previous-step output"}</span><button type="button" disabled={!index} onClick={()=>move(index,-1)}>Move up</button><button type="button" disabled={index===steps.length-1} onClick={()=>move(index,1)}>Move down</button><button type="button" disabled={steps.length===1} onClick={()=>setSteps(old=>old.filter((_,i)=>i!==index))}>Remove</button></div>)}<button type="button" className="mt-3" disabled={!agents.length} onClick={()=>setSteps(old=>[...old,agents[0].id])}>Add step</button></fieldset><p>The final step completes the run and provides its final output automatically.</p><button className="rounded bg-sky-700 px-4 py-2 font-bold text-white">Save draft workflow</button></form>}
