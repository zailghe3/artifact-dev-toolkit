"use client";
import {useState} from "react";
import type {ConnectionMigration} from "@/lib/workflow-provider-connection-migration";
import {workflowButton} from "@/components/WorkflowUi";

export function ProviderConnectionMigration({initial}:{initial:ConnectionMigration}){const [message,setMessage]=useState("");async function copy(value:string,label:string){await navigator.clipboard.writeText(value);setMessage(`${label} copied.`)}
 return <section className="mt-6 grid gap-5"><p role="status" className="rounded border p-3"><strong>Migration state:</strong> {initial.message}{message&&<> {message}</>}</p><dl className="grid grid-cols-[auto_1fr] gap-x-4 gap-y-2"><dt>Connection ID</dt><dd><code>{initial.connectionId}</code></dd><dt>Name</dt><dd>{initial.connectionName}</dd><dt>Model</dt><dd><code>{initial.configuredModel}</code></dd><dt>Destination</dt><dd><code>{initial.targetPath}</code></dd><dt>Secret provisioning</dt><dd>{initial.secretProvisioning==="resolved"?"Binding resolves; live validation shown in migration state.":"External provisioning required; existence has not been inferred."}</dd></dl><div><div className="flex items-center justify-between gap-3"><h2 className="text-lg font-bold">Canonical Git definition</h2><button className={workflowButton.secondary} onClick={()=>copy(initial.canonicalJson,"Canonical JSON")}>Copy JSON</button></div><pre className="mt-2 overflow-auto rounded bg-slate-950 p-4 text-sm text-slate-50"><code>{initial.canonicalJson}</code></pre></div><div><div className="flex items-center justify-between gap-3"><h2 className="text-lg font-bold">Cloudflare provider-secret binding</h2><button className={workflowButton.secondary} onClick={()=>copy(initial.secretRef,"Secret binding name")}>Copy binding name</button></div><p className="mt-2"><code>{initial.secretRef}</code></p><p className="mt-2 text-slate-600 dark:text-slate-300">The existing D1 credential cannot be exported. Separately provision the desired OpenAI credential into this binding through the Cloudflare control plane. ADT does not create or reveal the secret.</p></div>{initial.state==="git_ready_shadowing_d1"&&<div className="rounded border p-4"><h2 className="font-bold">Historical run compatibility retained</h2><p className="mt-2">Git now supplies this connection for new configuration and Workflow runs. The shadowed D1 row remains intact because pre-migration run snapshots may still require its original credential. Cleanup is deferred to a later explicitly designed phase.</p></div>}</section>}
