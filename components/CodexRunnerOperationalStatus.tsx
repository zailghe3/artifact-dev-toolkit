"use client";
import {useCallback,useEffect,useRef,useState} from "react";import {LocalizedTime} from "./LocalizedTime";import {workflowButton} from "./WorkflowUi";import type {RunnerJobList,RunnerJobSummary} from "@/lib/codex-runner-client";import type {SafeCodexConnectionStatus} from "@/lib/codex-runner-status";
const activeStates=new Set(["queued","running","cancelling"]),labels={queued:"Starting",running:"Running",cancelling:"Cancelling",completed:"Completed",failed:"Failed",cancelled:"Cancelled"} as const;
function JobCard({job,current=false}:{job:RunnerJobSummary;current?:boolean}){return <article className={`rounded-lg border p-4 ${current?"border-sky-600 bg-sky-50 dark:border-orange-400 dark:bg-slate-900":""}`}><div className="flex flex-wrap items-center justify-between gap-2"><code className="break-all text-sm">{job.jobId}</code><span className="rounded-full border px-2 py-1 text-sm font-semibold">{labels[job.state]}</span></div><dl className="mt-3 grid grid-cols-1 gap-1 text-sm sm:grid-cols-[auto_1fr] sm:gap-x-4"><dt>Environment</dt><dd><code>{job.environmentKey}</code></dd><dt>Created</dt><dd><LocalizedTime value={job.createdAt}/></dd><dt>Last updated</dt><dd><LocalizedTime value={job.updatedAt}/></dd>{job.reason&&<><dt>Safe failure reason</dt><dd>{job.reason.replaceAll("_"," ")}</dd></>}</dl></article>}
export function CodexRunnerOperationalStatus({connection}:{connection:SafeCodexConnectionStatus}){const[data,setData]=useState<RunnerJobList>(),[error,setError]=useState(false),[pending,setPending]=useState(false),inFlight=useRef(false);const refresh=useCallback(async()=>{if(inFlight.current)return;inFlight.current=true;setPending(true);try{const response=await fetch("/api/workflow-connections/codex-runner/jobs",{cache:"no-store"});if(!response.ok)throw new Error();setData(await response.json());setError(false)}catch{setError(true)}finally{inFlight.current=false;setPending(false)}},[]);useEffect(()=>{const timer=setTimeout(()=>void refresh(),0);return()=>clearTimeout(timer)},[refresh]);const active=data?.capacity.activeJobId?data.jobs.find(job=>job.jobId===data.capacity.activeJobId):undefined;useEffect(()=>{if(!active)return;const timer=setInterval(()=>void refresh(),4000);return()=>clearInterval(timer)},[active,refresh]);const recent=data?.jobs.filter(job=>!activeStates.has(job.state))??[];return <><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-3xl font-black">Codex Runner status</h1><p className="mt-1 text-slate-600 dark:text-slate-300">Read-only operational history from persisted Runner job state.</p></div><button className={workflowButton.secondary} onClick={()=>void refresh()} disabled={pending}>{pending?"Refreshing…":"Refresh"}</button></div><div className="mt-4 flex flex-wrap items-center justify-between gap-2 rounded border p-3"><span className="font-semibold">Connection readiness</span><span className="rounded-full border px-2 py-1 text-sm font-semibold">{connection.label}</span></div><p className="mt-4 rounded border p-3">Codex Runner executes one Workflow job at a time. Additional submissions while it is busy are rejected as <code>runner_busy</code> and handled by ADT retry policy.</p>{error&&<p role="alert" className="mt-4 rounded border border-red-600 p-3">Runner operational status is unavailable. Connection configuration and private transport details remain server-side.</p>}<section className="mt-6"><h2 className="text-xl font-bold">Current work</h2><div className="mt-3">{active?<JobCard job={active} current/>:<p className="rounded-lg border p-4">Runner is idle. There is no admitted Workflow job.</p>}</div></section><section className="mt-6"><h2 className="text-xl font-bold">Recent jobs</h2><div className="mt-3 grid gap-3">{recent.length?recent.map(job=><JobCard job={job} key={job.jobId}/>):<p className="rounded-lg border p-4">No terminal jobs are available in the current Runner history.</p>}</div></section></>}
