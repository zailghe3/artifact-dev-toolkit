"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function CodexEnvironmentForm(){const router=useRouter(),[error,setError]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget),response=await fetch("/api/workflow-codex-environments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({key:data.get("key"),name:data.get("name"),externalEnvironmentId:data.get("externalEnvironmentId"),enabled:data.get("enabled")==="on"})});if(!response.ok){setError("Environment reference could not be saved.");return;}event.currentTarget.reset();router.refresh();}return <form onSubmit={submit} className="mt-6 grid max-w-xl gap-3 rounded border p-4">{error&&<p role="alert">{error}</p>}<label>Key<input className="block w-full rounded border p-2" name="key" required maxLength={80} pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/></label><label>Display name<input className="block w-full rounded border p-2" name="name" required maxLength={120}/></label><label>Environment ID<input className="block w-full rounded border p-2" name="externalEnvironmentId" required maxLength={255}/></label><label><input name="enabled" type="checkbox"/> Enabled</label><button className="rounded bg-sky-700 p-2 font-bold text-white">Add environment reference</button></form>}
