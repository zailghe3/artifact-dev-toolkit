import {DiagnosticStatusBadge} from "@/components/DiagnosticStatusBadge";export function WorkflowStatusBadge({status}:{status:string}){const tone=status==="succeeded"?"positive":status==="failed"||status==="cancelled"?"negative":status==="cancelling"?"warning":"neutral";return <DiagnosticStatusBadge presentation={{label:status.replaceAll("_"," "),tone,description:`Workflow status: ${status}`}}/>}
