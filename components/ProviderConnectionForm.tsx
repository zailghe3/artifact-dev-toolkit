"use client";
import {useState} from "react";
import {canTestProviderConnection,providerConnectionTestFeedback} from "@/lib/provider-connection-presentation";
export function ProviderConnectionForm({model,ready,storageAvailable}:{model:string;ready:boolean;storageAvailable:boolean}){const[currentModel,setModel]=useState(model),[configured,setConfigured]=useState(ready),[status,setStatus]=useState(ready?"Ready":"Not configured"),[busy,setBusy]=useState(false),[testing,setTesting]=useState(false),[testResult,setTestResult]=useState("");async function save(form:FormData){setBusy(true);setTestResult("");const credential=String(form.get("credential")??"");const response=await fetch("/api/workflow-connections/openai-primary",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({model:currentModel,...(credential?{credential}:{})})});setConfigured(response.ok);setStatus(response.ok?"Ready":"Unable to save");setBusy(false);}async function remove(){setBusy(true);setTestResult("");const response=await fetch("/api/workflow-connections/openai-primary",{method:"DELETE"});if(response.ok)setConfigured(false);setStatus(response.ok?"Not configured":"Unable to remove");setBusy(false);}async function testConnection(){setBusy(true);setTesting(true);setTestResult("");try{const response=await fetch("/api/workflow-connections/openai-primary/test",{method:"POST"}),value:unknown=await response.json();setTestResult(providerConnectionTestFeedback(value,response.ok));}catch{setTestResult(providerConnectionTestFeedback(undefined,false));}finally{setTesting(false);setBusy(false);}}return <form action={save} className="mt-4 grid gap-3"><label>Model<input className="ml-2 rounded border p-2" name="model" maxLength={120} required value={currentModel} onChange={e=>setModel(e.target.value)}/></label><label>API key<input autoComplete="off" className="ml-2 rounded border p-2" name="credential" type="password" maxLength={8192} disabled={!storageAvailable}/></label><p>{status}</p>{testResult&&<p role="status">{testResult}</p>}{!storageAvailable&&<p>Provider secret storage is unavailable.</p>}<div className="flex flex-wrap gap-2"><button className="rounded border p-2" disabled={busy||!storageAvailable}>Configure or Edit</button><button className="rounded border p-2" disabled={!canTestProviderConnection({busy,configured})} type="button" onClick={testConnection}>{testing?"Testing…":"Test connection"}</button>{configured&&<button className="rounded border p-2" disabled={busy} type="button" onClick={remove}>Remove credential/configuration</button>}</div></form>}
