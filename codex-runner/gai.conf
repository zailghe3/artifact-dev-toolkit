# Complete glibc RFC 3484 precedence table, with IPv4-mapped destinations preferred.
# Any precedence directive replaces the built-in table, so every default entry is retained.
precedence  ::1/128       50
precedence  ::/0          40
precedence  2002::/16     30
precedence  ::/96         20
precedence  ::ffff:0:0/96 100
