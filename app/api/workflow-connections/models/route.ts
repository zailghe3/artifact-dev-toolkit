import {NextResponse} from "next/server";import {z} from "zod";import {requireApiRepositoryAccess} from "@/lib/auth";import {noStoreHeaders} from "@/lib/auth-core";import {readBoundedJson,workflowError} from "@/lib/workflow-http";import {listOpenAIModels} from "@/lib/openai-models";
const input=z.object({credential:z.string().min(1).max(8192)}).strict();function sameOrigin(r:Request){const origin=r.headers.get("origin");return !!origin&&origin===new URL(r.url).origin;}
export async function POST(request:Request){const auth=await requireApiRepositoryAccess(request);if(auth instanceof Response)return auth;try{if(!sameOrigin(request))return NextResponse.json({code:"invalid_origin",error:"Request origin is invalid."},{status:403,headers:noStoreHeaders});const {credential}=input.parse(await readBoundedJson(request,9216));return NextResponse.json({models:await listOpenAIModels(credential)},{headers:noStoreHeaders});}catch(error){return workflowError(error);}}
