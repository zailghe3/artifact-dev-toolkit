import {NextResponse} from "next/server";import {requireApiRepositoryAccess} from "@/lib/auth";import {noStoreHeaders} from "@/lib/auth-core";import {listConnectionDescriptors} from "@/lib/workflow-connections";import {getWorkflowProviderConnectionStore} from "@/lib/workflow-services";
export async function GET(request:Request){const auth=await requireApiRepositoryAccess(request);if(auth instanceof Response)return auth;const providers=await getWorkflowProviderConnectionStore();return NextResponse.json({connections:[...listConnectionDescriptors(),...await providers.listSafeDescriptors()]},{headers:noStoreHeaders});}
