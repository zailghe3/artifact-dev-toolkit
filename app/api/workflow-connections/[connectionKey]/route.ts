import {NextResponse} from "next/server";import {z} from "zod";import {requireApiRepositoryAccess} from "@/lib/auth";import {noStoreHeaders} from "@/lib/auth-core";import {getWorkflowProviderConnectionStore} from "@/lib/workflow-services";import {readBoundedJson,workflowError} from "@/lib/workflow-http";
const input=z.object({model:z.string().trim().min(1).max(120),credential:z.string().min(1).max(8192).optional()}).strict();
function sameOrigin(request:Request){const origin=request.headers.get("origin");return !!origin&&origin===new URL(request.url).origin;}
export async function PUT(request:Request,{params}:{params:Promise<{connectionKey:string}>}){const auth=await requireApiRepositoryAccess(request);if(auth instanceof Response)return auth;try{if(!sameOrigin(request))return NextResponse.json({code:"invalid_origin",error:"Request origin is invalid."},{status:403,headers:noStoreHeaders});const {connectionKey}=await params;if(connectionKey!=="openai-primary")return NextResponse.json({code:"not_found",error:"Connection not found."},{status:404,headers:noStoreHeaders});const body=input.parse(await readBoundedJson(request,9216));const connection=await(await getWorkflowProviderConnectionStore()).upsertConnection({connectionKey,adapter:"openai-responses",model:body.model,...(body.credential?{credential:body.credential}:{})});return NextResponse.json({connection},{headers:noStoreHeaders});}catch(error){return workflowError(error);}}
export async function DELETE(request:Request,{params}:{params:Promise<{connectionKey:string}>}){const auth=await requireApiRepositoryAccess(request);if(auth instanceof Response)return auth;if(!sameOrigin(request))return NextResponse.json({code:"invalid_origin",error:"Request origin is invalid."},{status:403,headers:noStoreHeaders});const {connectionKey}=await params;if(connectionKey!=="openai-primary")return NextResponse.json({code:"not_found",error:"Connection not found."},{status:404,headers:noStoreHeaders});await(await getWorkflowProviderConnectionStore()).deleteConnection(connectionKey);return new Response(null,{status:204,headers:noStoreHeaders});}
