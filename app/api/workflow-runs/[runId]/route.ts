import {NextResponse} from "next/server";import {requireApiRepositoryAccess} from "@/lib/auth";import {noStoreHeaders} from "@/lib/auth-core";import {workflowError} from "@/lib/workflow-http";import {getWorkflowRunDetail} from "@/lib/workflow-run-detail";
export async function GET(request:Request,{params}:{params:Promise<{runId:string}>}){const auth=await requireApiRepositoryAccess(request);if(auth instanceof Response)return auth;try{const value=await getWorkflowRunDetail((await params).runId);return value?NextResponse.json(value,{headers:noStoreHeaders}):NextResponse.json({error:"Run not found",code:"not_found"},{status:404,headers:noStoreHeaders});}catch(error){return workflowError(error);}}
