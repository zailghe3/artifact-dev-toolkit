import { requireApiRepositoryAccess } from "@/lib/auth";
import { getArtifactWithRevision, proposeArtifactDeletion } from "@/lib/artifacts";
import { handleDeletionProposal } from "@/lib/deletion-route-handler";
import {NextResponse} from "next/server";import {noStoreHeaders} from "@/lib/auth-core";import {createWorkflowDefinitionRepository} from "@/lib/workflow-services";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) {const id=(await params).id;return handleDeletionProposal(request,id,{authorize:async request=>{const auth=await requireApiRepositoryAccess(request);if(auth instanceof Response)return auth;const references=(await createWorkflowDefinitionRepository(auth.access).listAgents()).filter(({definition})=>definition.prompt.source==="artifact"&&definition.prompt.artifactId===id);return references.length?NextResponse.json({code:"artifact_in_use",error:`This prompt is referenced by ${references.length} Agents and cannot be deleted. Remove or replace those references first.`,agents:references.map(({definition})=>({id:definition.id,name:definition.name}))},{status:409,headers:noStoreHeaders}):auth},load:getArtifactWithRevision,propose:proposeArtifactDeletion});}
