import {DefinitionCatalogue} from "@/components/DefinitionCatalogue";import {requireRepositoryAccess} from "@/lib/auth";import {createWorkflowDefinitionRepository} from "@/lib/workflow-services";import {WorkflowSectionHeader} from "@/components/WorkflowUi";
export default async function Page(){const {access}=await requireRepositoryAccess("/workflows/agents"),agents=await createWorkflowDefinitionRepository(access).listAgents();return <><WorkflowSectionHeader title="Agents" description="Configure reusable Agents and their provider connections." action={{href:"/workflows/agents/new",label:"New agent"}}/>{agents.length?<DefinitionCatalogue kind="agent" items={agents}/>:<div className="rounded-lg border border-dashed p-6"><h2 className="font-bold">No agents yet.</h2><p className="mt-1 text-slate-600 dark:text-slate-300">Agents provide the instructions and provider connection used by Workflow steps.</p></div>}</>}
