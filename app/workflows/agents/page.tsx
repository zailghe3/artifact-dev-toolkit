import Link from "next/link";import {requireRepositoryAccess} from "@/lib/auth";import {createWorkflowDefinitionRepository} from "@/lib/workflow-services";export default async function Page(){const {access}=await requireRepositoryAccess("/workflows/agents"),agents=await createWorkflowDefinitionRepository(access).listAgents();return <><div className="flex justify-between"><h1 className="text-3xl font-black">Agents</h1><Link href="/workflows/agents/new">Create agent</Link></div>{agents.length?<ul className="mt-6 grid gap-3">{agents.map(({definition})=><li key={definition.id}><Link className="font-bold underline" href={`/workflows/agents/${definition.id}`}>{definition.name}</Link><p>{definition.description}</p></li>)}</ul>:<p className="mt-4">No draft agents yet.</p>}</>}
