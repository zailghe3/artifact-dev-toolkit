import {WorkflowAgentEditor} from "@/components/WorkflowAgentEditor";import {listWorkflowConnectionDescriptors,readCodexRunnerCatalog} from "@/lib/workflow-services";export default async function Page(){const snapshot=await readCodexRunnerCatalog(),connections=await listWorkflowConnectionDescriptors(snapshot),catalog=snapshot.models?{environments:snapshot.environments,models:snapshot.models}:undefined;return <><h1 className="text-3xl font-black">New agent</h1><WorkflowAgentEditor connections={connections} codexCatalog={catalog}/></>}
