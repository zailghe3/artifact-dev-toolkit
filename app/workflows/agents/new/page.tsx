import {WorkflowAgentEditor} from "@/components/WorkflowAgentEditor";import {listWorkflowConnectionDescriptors,readCodexRunnerCatalog} from "@/lib/workflow-services";export default async function Page(){const snapshot=await readCodexRunnerCatalog(),connections=await listWorkflowConnectionDescriptors(snapshot);return <><h1 className="text-3xl font-black">New agent</h1><WorkflowAgentEditor connections={connections} codexSnapshot={snapshot}/></>}
