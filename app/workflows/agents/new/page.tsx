import {WorkflowAgentEditor} from "@/components/WorkflowAgentEditor";import {listWorkflowConnectionDescriptors,readCodexRunnerCatalog} from "@/lib/workflow-services";export default async function Page(){const [connections,catalog]=await Promise.all([listWorkflowConnectionDescriptors(),readCodexRunnerCatalog()]);return <><h1 className="text-3xl font-black">New agent</h1><WorkflowAgentEditor connections={connections} codexCatalog={catalog.available?catalog:undefined}/></>}
