import {WorkflowAgentEditor} from "@/components/WorkflowAgentEditor";import {listWorkflowConnectionDescriptors,readCodexRunnerCatalog} from "@/lib/workflow-services";import {requireRepositoryAccess} from "@/lib/auth";export default async function Page(){const {access}=await requireRepositoryAccess("/workflows/agents/new"),snapshot=await readCodexRunnerCatalog(),connections=await listWorkflowConnectionDescriptors(snapshot,access);return <><h1 className="text-3xl font-black">New agent</h1><WorkflowAgentEditor connections={connections} codexSnapshot={snapshot}/></>}
