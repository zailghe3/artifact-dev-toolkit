import {notFound} from "next/navigation";import {WorkflowDefinitionEditor} from "@/components/WorkflowDefinitionEditor";import {requireRepositoryAccess} from "@/lib/auth";import {createWorkflowDefinitionRepository,createWorkflowLayoutRepository} from "@/lib/workflow-services";export default async function Page({params}:{params:Promise<{workflowId:string}>}){const {workflowId}=await params,{access}=await requireRepositoryAccess(`/workflows/definitions/${workflowId}/edit`),repo=createWorkflowDefinitionRepository(access),value=await repo.getWorkflow(workflowId);if(!value)notFound();const layout=await createWorkflowLayoutRepository(access).getLayout(workflowId),agents=(await repo.listAgents()).map(x=>({id:x.definition.id,name:x.definition.name}));const reusableWorkflows=(await repo.listWorkflows()).filter(x=>x.definition.schemaVersion===2&&x.definition.exposableAsBlock&&x.definition.id!==workflowId).map(x=>({id:x.definition.id,name:x.definition.name,description:x.definition.description}));return <><h1 className="text-3xl font-black">Edit workflow {value.definition.name}</h1><WorkflowDefinitionEditor agents={agents} reusableWorkflows={reusableWorkflows} initial={value.definition} fileSha={value.fileSha} initialLayout={layout}/></>}
