import {DefinitionCatalogue} from "@/components/DefinitionCatalogue";import {requireRepositoryAccess} from "@/lib/auth";import {createWorkflowDefinitionRepository} from "@/lib/workflow-services";import {WorkflowSectionHeader} from "@/components/WorkflowUi";
export default async function Page(){const {access}=await requireRepositoryAccess("/workflows/definitions"),items=await createWorkflowDefinitionRepository(access).listWorkflows();return <><WorkflowSectionHeader title="Workflows" description="Create and manage the ordered Agent workflows available to run." action={{href:"/workflows/definitions/new",label:"New workflow"}}/>{items.length?<DefinitionCatalogue kind="workflow" items={items}/>:<div className="adt-empty-state"><h2 className="font-bold">No workflows yet.</h2><p className="mt-1 text-slate-600 dark:text-slate-300">Workflows arrange Agents into ordered, reusable steps.</p></div>}</>}
