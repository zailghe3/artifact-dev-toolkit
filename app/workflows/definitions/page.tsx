import Link from "next/link";import {requireRepositoryAccess} from "@/lib/auth";import {createWorkflowDefinitionRepository} from "@/lib/workflow-services";export default async function Page(){const {access}=await requireRepositoryAccess("/workflows/definitions"),items=await createWorkflowDefinitionRepository(access).listWorkflows();return <><div className="flex justify-between"><h1 className="text-3xl font-black">Workflow definitions</h1><Link href="/workflows/definitions/new">Create workflow</Link></div>{items.length?<ul className="mt-6 grid gap-3">{items.map(({definition})=><li key={definition.id}><Link className="font-bold underline" href={`/workflows/definitions/${definition.id}`}>{definition.name}</Link><p>{definition.description}</p></li>)}</ul>:<p className="mt-4">No workflow definitions yet.</p>}</>}
