import {PageHeader} from "@/components/Ui";import {ProviderConnectionEditor} from "@/components/ProviderConnectionEditor";import {getWorkflowProviderConnectionStore} from "@/lib/workflow-services";import {requireRepositoryAccess} from "@/lib/auth";import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{connectionKey:string}>}){const {access}=await requireRepositoryAccess("/workflows/connections"),connection=await(await getWorkflowProviderConnectionStore(access)).getSafeDescriptor((await params).connectionKey);if(!connection||connection.management!=="git"||!connection.repositoryRevision)notFound();return <><PageHeader title={`Edit ${connection.name}`} description="Update provider configuration and manage its credential."/><ProviderConnectionEditor initial={{key:connection.key,name:connection.name,model:connection.defaultModel??"",runtime:connection.adapter as "openai-responses"|"openai-agents",repositoryRevision:connection.repositoryRevision,configured:connection.configured,credentialSource:connection.credentialSource}}/></>}
