import {ProviderConnectionEditor} from "@/components/ProviderConnectionEditor";import {getWorkflowProviderConnectionStore} from "@/lib/workflow-services";import {notFound} from "next/navigation";
export default async function Page({searchParams}:{searchParams:Promise<{duplicate?:string}>}){const {duplicate}=await searchParams;if(!duplicate)return <><h1 className="text-3xl font-black">New workflow connection</h1><ProviderConnectionEditor/></>;const store=await getWorkflowProviderConnectionStore(),source=await store.getSafeDescriptor(duplicate);if(!source)notFound();let proposed=`${source.key}-copy`,number=2;while(await store.getSafeDescriptor(proposed))proposed=`${source.key}-copy-${number++}`;return <><h1 className="text-3xl font-black">Duplicate workflow connection</h1><ProviderConnectionEditor duplicateSource={source.key} initial={{key:proposed,name:`${source.name} copy`,model:source.defaultModel??""}}/></>}
