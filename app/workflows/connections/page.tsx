import { listConnectionDescriptors } from "@/lib/workflow-connections";
export default function Page(){const connections=listConnectionDescriptors();return <><h1 className="text-3xl font-black">Workflow connections</h1>{connections.map(c=><article className="mt-6 rounded border p-4" key={c.key}><h2 className="text-xl font-bold">{c.name}</h2><dl className="mt-2 grid grid-cols-2 gap-2"><dt>Adapter</dt><dd>{c.adapter}</dd><dt>Readiness</dt><dd>{c.enabled?"Ready":"Not configured"}</dd><dt>Invocation</dt><dd>{c.capabilities.asynchronous?"Asynchronous supported":"Synchronous only"}</dd><dt>Cancellation</dt><dd>{c.capabilities.cancellation?"Supported":"Unsupported"}</dd></dl><p className="mt-3 text-sm">This deterministic local test facility is not an AI provider.</p></article>)}</>}
