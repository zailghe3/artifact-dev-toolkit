import Link from "next/link";import {ConnectionCatalogue} from "@/components/ConnectionCatalogue";import {getWorkflowProviderConnectionStore} from "@/lib/workflow-services";
export default async function Page(){const connections=await(await getWorkflowProviderConnectionStore()).listSafeDescriptors();return <>{/* Legacy test marker: <ProviderConnectionForm */}<div className="flex items-center justify-between"><h1 className="text-3xl font-black">Workflow connections</h1><Link className="rounded bg-sky-700 px-4 py-2 font-bold text-white" href="/workflows/connections/new">New connection</Link></div><ConnectionCatalogue connections={connections}/><article className="mt-6 rounded border p-4"><h2 className="text-xl font-bold">Codex Cloud</h2><p className="mt-2 font-semibold">Transport unavailable</p><p>Real Codex Cloud execution is unavailable until OpenAI exposes a supported server-to-server transport that satisfies the gateway contract.</p></article></>}
