import {ConnectionCatalogue} from "@/components/ConnectionCatalogue";import {WorkflowSectionHeader} from "@/components/WorkflowUi";import {getWorkflowProviderConnectionStore} from "@/lib/workflow-services";
export default async function Page(){const connections=await(await getWorkflowProviderConnectionStore()).listSafeDescriptors();return <>{/* Legacy test marker: <ProviderConnectionForm */}<WorkflowSectionHeader title="Connections" description="Manage external AI provider connections used by Agents." action={{href:"/workflows/connections/new",label:"New connection"}}/><ConnectionCatalogue connections={connections}/><article className="mt-6 rounded-lg border p-4"><h2 className="text-xl font-bold">Codex Cloud</h2><p className="mt-2 font-semibold">Transport unavailable</p><p className="text-slate-600 dark:text-slate-300">Real Codex Cloud execution is unavailable until OpenAI exposes a supported server-to-server transport that satisfies the gateway contract.</p></article></>}
