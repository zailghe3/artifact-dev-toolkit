import Link from "next/link";import {getWorkflowRunStorage} from "@/lib/workflow-services";import {WorkflowRunsTable,type WorkflowRunTableRow} from "@/components/WorkflowRunsTable";import type {WorkflowRun} from "@/lib/workflow-storage";const row=(run:WorkflowRun):WorkflowRunTableRow=>({id:run.id,workflowName:run.workflowSnapshot.name,status:run.status,createdAt:run.createdAt,startedAt:run.startedAt,completedAt:run.completedAt,currentStepId:run.currentStepId});export default async function WorkflowsPage(){const runs=await (await getWorkflowRunStorage()).listRuns(10),active=runs.filter(run=>!["succeeded","failed","cancelled"].includes(run.status));return <><h1 className="text-3xl font-black">Workflows</h1><p className="mt-2">Run durable, deterministic sequences of configured agents.</p><p className="mt-4"><Link className="underline" href="/workflows/definitions">Start workflow</Link> · <Link className="underline" href="/workflows/agents">Agents</Link> · <Link className="underline" href="/workflows/connections">Connections</Link></p><section className="mt-8"><h2 className="text-xl font-bold">Active runs</h2>{active.length?<WorkflowRunsTable runs={active.map(row)}/>:<p>There are no active runs.</p>}</section><section className="mt-8"><h2 className="text-xl font-bold">Recent runs</h2>{runs.length?<WorkflowRunsTable runs={runs.map(row)}/>:<p>There are no workflow runs yet.</p>}</section></>}
