import {WorkflowSectionHeader} from "@/components/WorkflowUi";
import {WorkflowRunsTable} from "@/components/WorkflowRunsTable";
import {getWorkflowRunStorage} from "@/lib/workflow-services";
export default async function Page(){const runs=await (await getWorkflowRunStorage()).listRuns(50);return <><WorkflowSectionHeader title="Workflow runs" description="Review current and completed Workflow executions."/>{runs.length?<WorkflowRunsTable runs={runs.map(run=>({id:run.id,workflowName:run.workflowSnapshot.name,status:run.status,createdAt:run.createdAt,startedAt:run.startedAt,completedAt:run.completedAt,currentStepId:run.currentStepId}))}/>:<div className="rounded-lg border border-dashed p-6"><h2 className="font-bold">No workflow runs yet.</h2><p className="mt-1 text-slate-600 dark:text-slate-300">Runs appear here after a Workflow is started.</p></div>}</>}
