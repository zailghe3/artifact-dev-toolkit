import {CodexEnvironmentForm} from "@/components/CodexEnvironmentForm";import {getWorkflowCodexEnvironmentStore} from "@/lib/workflow-services";
export default async function Page(){const environments=await(await getWorkflowCodexEnvironmentStore()).list();return <><h1 className="text-3xl font-black">Codex environments</h1><p className="mt-2">Safe references only. Repository access, setup, secrets, and GitHub integration remain configured in Codex Cloud.</p><ul className="mt-6 grid gap-3">{environments.map(item=><li className="rounded border p-4" key={item.key}><h2 className="font-bold">{item.name}</h2><p>Environment ID: <code>{item.externalEnvironmentId}</code></p><p>{item.enabled?"Ready":"Disabled"}</p></li>)}</ul><CodexEnvironmentForm/></>}
